{
  "name": "vant-vue-ledger",
  "version": "1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "preview": "vite preview --host",
    "cap:init": "npx cap init Ledger com.example.ledger",
    "cap:add:android": "npx cap add android",
    "build": "vite build",
    "cap:copy": "npx cap copy",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "android:release": "cd android && (gradlew.bat assembleRelease || ./gradlew assembleRelease)"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.3",
    "echart": "^0.1.3",
    "echarts": "^6.0.0",
    "vant": "^4.9.21",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
